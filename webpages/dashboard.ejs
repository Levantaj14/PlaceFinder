<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Your dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
            crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <script src="/dashboardScript.js"></script>
</head>

<body>

<%- include('partials/navbar.ejs', { login: loggedIn, username, pageName: 'dashboard' }) %>

<div class="container mt-3 mb-4">
    <%- include('partials/message.ejs', { alerting, message }) %>
    <h2>
        Hi, <%= username; %>!
        <% if (isAdmin === "true") { %>
            <span class="badge bg-secondary">admin</span>
        <% } %>
    </h2>
    <h5>Make the changes that you want to make</h5>
    <div class="mb-3 mt-4 row">
        <div class="col-sm-3">
            <ul class="mb-4 nav nav-pills nav-justified flex-column">
                <li class="nav-item">
                    <a class="nav-link active" data-bs-toggle="tab" href="#yourProfile">Your profile</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" data-bs-toggle="tab" href="#security">Security</a>
                </li>
                <% if (isAdmin === "true") { %>
                    <li class="nav-item">
                        <a class="nav-link" data-bs-toggle="tab" href="#listings">All listings</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" data-bs-toggle="tab" href="#accounts">Accounts</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" data-bs-toggle="tab" href="#listings">Your listings</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" data-bs-toggle="tab" href="#admin">Admin</a>
                    </li>
                <% } %>
                <li class="nav-item">
                    <a class="nav-link" data-bs-toggle="tab" href="#dangerZone">Danger zone</a>
                </li>
            </ul>
        </div>
        <div class="col">
            <div class="tab-content">
                <div id="yourProfile" class="container tab-pane fade active show">
                    <h4>Edit your profile</h4>
                    <form action="/account/changeUserData" method="post" id="profileForm">
                        <div class="mb-3 mt-3 row">
                            <div class="col">
                                <label class="form-label" for="name">Name</label>
                                <input type="text" id="name" class="form-control" name="name" value="<%= fullName; %>"
                                       required>
                                <div class="invalid-feedback" id="nameError">
                                    This field is mandatory.
                                </div>
                            </div>
                        </div>
                        <div class="mb-3 mt-3 row">
                            <div class="col">
                                <label class="form-label" for="username">Username</label>
                                <input type="text" id="username" class="form-control" name="username"
                                       value="<%= username %>" required>
                                <div class="invalid-feedback" id="usernameError">
                                    This field is mandatory.
                                </div>
                            </div>
                        </div>
                        <button type="submit" class="btn btn-primary" id="profileButton">Change</button>
                    </form>
                </div>
                <div id="security" class="container tab-pane fade">
                    <h4>Security</h4>
                    <h6>Change your password</h6>
                    <form action="/account/resetPassword" method="post" id="passwordForm">
                        <div class="mb-3 mt-3 row">
                            <div class="col">
                                <label class="form-label" for="password">Enter your current password</label>
                                <input type="password" id="password" class="form-control" name="password" required>
                            </div>
                        </div>
                        <div class="mb-3 mt-3 row">
                            <div class="col">
                                <label class="form-label" for="newPassword">Enter your new password</label>
                                <input type="password" id="newPassword" class="form-control" name="newPassword"
                                       required>
                                <div id="passwordHelpBlock" class="form-text">
                                    The password must be at least 8 characters long and must contain letters and
                                    numbers.
                                </div>
                                <div class="invalid-feedback" id="passwordError">
                                    The password must be at least 8 characters long.
                                </div>
                            </div>
                        </div>
                        <div class="mb-3 mt-3 row">
                            <div class="col">
                                <label class="form-label" for="newPasswordAgain">Enter your new password again</label>
                                <input type="password" id="newPasswordAgain" class="form-control"
                                       name="newPasswordAgain" required>
                                <div class="invalid-feedback" id="passwordAgainError">
                                    The password must be at least 8 characters long.
                                </div>
                            </div>
                        </div>
                        <button type="submit" class="btn btn-primary" id="passwordButton">Change your password</button>
                    </form>
                    <div class="mb-3 mt-5 row">
                        <div class="col">
                            <% if (twoFact) { %>
                                <h6>
                                    2-factor authentication
                                    <span id="2factIndicator" class="badge rounded-pill bg-success">ON</span>
                                </h6>
                                <p id="2FactText">Your account is more secure.</p>
                                <button id="2FactButtonOff" type="button" class="btn btn-danger" data-bs-toggle="modal"
                                        data-bs-target="#twoFactOff">
                                    Turn it off
                                </button>
                            <% } else { %>
                                <h6>
                                    2-factor authentication
                                    <span class="badge rounded-pill bg-danger">OFF</span>
                                </h6>
                                <p>For an extra layer of security we recommend that you turn this on.</p>
                                <a class="btn btn-primary" id="2FactorOn" href="/account/2factorAuthenticationOn">Turn
                                    it on</a>
                            <% } %>
                        </div>
                    </div>
                </div>
                <div id="listings" class="container tab-pane fade">
                    <% if (isAdmin === "true") { %>
                        <h4>All listings</h4>
                    <% } else { %>
                        <h4>Your listings</h4>
                    <% } %>
                    <% if (listings.length > 0) { %>
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead>
                                <tr>
                                    <th>Title</th>
                                    <% if (isAdmin === "true") { %>
                                        <th>Owner</th>
                                    <% } %>
                                    <th></th>
                                    <th></th>
                                </tr>
                                </thead>
                                <tbody>
                                <% listings.forEach((listing) => { %>
                                    <tr id="<%= listing._id %>-row">
                                        <td><%= listing.title %></td>
                                        <% if (isAdmin === "true") { %>
                                            <td><%= listing.user %></td>
                                        <% } %>
                                        <td>
                                            <a class="btn btn-outline-primary btn-sm"
                                               href="/listings/showListing?id=<%= listing._id %>">
                                                Edit listing
                                            </a>
                                        </td>
                                        <td>
                                            <button type="button" class="btn btn-outline-danger btn-sm deleteListing"
                                                    data-id="<%= listing._id %>">
                                                Delete listing
                                            </button>
                                        </td>
                                    </tr>
                                <% }) %>
                                </tbody>
                            </table>
                        </div>
                    <% } else { %>
                        <h6 class="mt-3">It looks like you haven't posted a listing.</h6>
                    <% } %>
                </div>
                <% if (isAdmin === "true") { %>
                    <div id="accounts" class="container tab-pane fade">
                        <h4>Accounts</h4>
                        <% if (users.length > 0) { %>
                            <div class="table-responsive">
                                <table class="table table-hover">
                                    <thead>
                                    <tr>
                                        <th>Name</th>
                                        <th>Username</th>
                                        <th></th>
                                        <th></th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    <% users.forEach((user) => { %>
                                        <tr id="<%= user.username %>-row">
                                            <% if (user.username !== username) { %>
                                                <% if (user.isAdmin === 'true') { %>
                                                    <td>
                                                        <%= user.name %>
                                                        <span id="<%= user.username %>-badge"
                                                              class="badge bg-secondary">admin</span>
                                                    </td>
                                                    <td><%= user.username %></td>
                                                    <td>
                                                        <button type="button"
                                                                class="btn btn-outline-danger btn-sm revokeAdmin"
                                                                data-user="<%= user.username %>">
                                                            Revoke admin
                                                        </button>
                                                    </td>
                                                <% } else if (user.isAdmin === 'false') { %>
                                                    <td>
                                                        <%= user.name %>
                                                        <span id="<%= user.username %>-badge"></span>
                                                    </td>
                                                    <td><%= user.username %></td>
                                                    <td>
                                                        <button type="button"
                                                                class="btn btn-outline-primary btn-sm makeAdmin"
                                                                data-user="<%= user.username %>">
                                                            Make admin
                                                        </button>
                                                    </td>
                                                <% } else { %>
                                                    <td>
                                                        <%= user.name %>
                                                        <span id="<%= user.username %>-badge" class="badge bg-primary">Requested to become an admin</span>
                                                    </td>
                                                    <td><%= user.username %></td>
                                                    <td>
                                                        <button type="button"
                                                                class="btn btn-outline-primary btn-sm makeAdmin"
                                                                data-user="<%= user.username %>">
                                                            Make admin
                                                        </button>
                                                    </td>
                                                <% } %>
                                                <td>
                                                    <button type="button"
                                                            class="btn btn-outline-danger btn-sm deleteAccount"
                                                            data-username="<%= user.username %>">
                                                        Delete account
                                                    </button>
                                                </td>
                                        </tr>
                                    <% }}) %>
                                    </tbody>
                                </table>
                            </div>
                        <% } else { %>
                            <h6 class="mt-3">It looks like there is no other account.</h6>
                            <p>I guess we are not so popular.</p>
                        <% } %>
                    </div>
                <% } else { %>
                    <div id="admin" class="container tab-pane fade">
                        <h4>Become an admin</h4>
                        <p>If you think you deserve to become an admin, you can request to become one by pressing the
                            button bellow.</p>
                        <% if (isAdmin === "false") { %>
                            <button id="preRequestButton" type="button" class="btn btn-primary" data-bs-toggle="modal"
                                    data-bs-target="#beAware">
                                Request to become an admin
                            </button>
                        <% } else { %>
                            <button type="button" class="btn btn-primary disabled">
                                Already requested
                            </button>
                        <% } %>
                    </div>
                <% } %>
                <div id="dangerZone" class="container tab-pane fade">
                    <h4>Danger zone</h4>
                    <p>If you would like you can delete your account, but we don't really recommend it.</p>
                    <button type="button" class="btn btn-danger" data-bs-toggle="modal"
                            data-bs-target="#accountDelete">Delete account
                    </button>
                </div>
            </div>
        </div>
    </div>
    <%- include('partials/toastNotification.ejs') %>
</div>

<% if (isAdmin === "false") { %>
    <div class="modal fade" id="beAware">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h4 class="modal-title">Be aware <%= username %>! </h4>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    With great power comes great responsibility! Send this request only if you think that you can make
                    good choices.
                </div>
                <div class="modal-footer">
                    <button id="requestAdmin" type="button" class="btn btn-danger" data-bs-dismiss="modal"
                            data-user="<%= username %>">Send the request
                    </button>
                </div>

            </div>
        </div>
    </div>
<% } %>
<% if (twoFact) { %>
    <div class="modal fade" id="twoFactOff">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h4 class="modal-title">Are you sure <%= username %>?</h4>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    Turning off 2-factor authentication will make your account less secure.
                </div>
                <div class="modal-footer">
                    <button id="turn2FactOff" type="button" class="btn btn-danger" data-bs-dismiss="modal"
                            data-user="<%= username %>">Turn it off anyway
                    </button>
                </div>

            </div>
        </div>
    </div>
<% } %>
<div class="modal fade" id="accountDelete">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Be aware <%= username %>! </h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                Deleting your account means that all of your listings and messages will also be deleted. Are you sure
                you want to proceed?
            </div>
            <div class="modal-footer">
                <form action="/account/deleteMyAccount" method="post">
                    <button type="submit" class="btn btn-danger" data-bs-dismiss="modal">Yes, delete my account</button>
                </form>
            </div>

        </div>
    </div>
</div>
</body>

</html>
